# Core: none — storage is stdlib sqlite3; tests run with zero third-party deps
# Bot runtime (needed only to actually run the Telegram bot, not for tests)
python-telegram-bot>=21.0
httpx>=0.27
# Mini App web server (needed only to run ph.web.main, not for tests)
fastapi>=0.111
uvicorn>=0.30
# Optional real providers (selected via env; stubs used if keys absent)
openai>=1.40          # LLM + STT (gpt-4o-transcribe) + TTS (gpt-4o-mini-tts)
anthropic>=0.34       # alt LLM
# Email escalation (SMTP uses stdlib; Resend/SES optional)
# Dev / test
pytest>=8.0
